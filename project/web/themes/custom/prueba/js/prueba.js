/**
 * @file
 * prueba behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.prueba = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
